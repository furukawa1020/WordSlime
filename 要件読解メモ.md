# WordSlime 要件読解・調査メモ

## 読み込み確認

- 対象ファイル: `C:\Projects\WordSlime\要件定義書.md`
- 読み込み方法: PowerShell の `Get-Content -Encoding UTF8` で確認
- 行数: 1729 行
- 文字化け確認: 先頭、中盤、末尾まで日本語が正常に読めることを確認
- 注意: 既定エンコーディングで読むと文字化けしたため、以後このファイルは必ず `UTF-8` 指定で扱う

## 一文での理解

WordSlime は、入力された言葉を意味解析せず、文字列の長さ、反復、記号、文字種、リズムから物理パラメータへ変換し、WebGPU 粒子シミュレーションと Web Audio API によって「ことばの粘菌水槽」として生かすブラウザ玩具。

## 絶対に外さない核

- 言葉を「読む」のではなく、読む前に「溶かす」
- AI診断、感情推定、文章理解、要約、チャットに寄せない
- 文字列の意味ではなく、形、長さ、反復、記号、リズムを物理へ変換する
- 触って気持ち悪く、でもかわいい
- 作品として見た瞬間に「何これ」となる
- 実用アプリ、研究ツール、メンタルヘルスアプリに見せない
- 完全ローカル、ログインなし、サーバーなし、外部APIなし
- WebGPU と WGSL を作品の技術核にする
- 音源ファイルは使わず、Web Audio API で音を生成する

## 章別読解

### 1. プロダクト定義

WordSlime は、ユーザー入力を意味解析するソフトではなく、文字列の形態的特徴を物理パラメータへ変換するソフト。  
「ことばを飼う」ための変なソフトであり、AI、日記、タスク管理、研究基盤ではない。WebGPU は粒子、群れ、流体風表現に使い、Web Audio API は文字が鳴く、粒子がざわめく表現に使う。

### 2. 最終コンセプト

ユーザーが言葉を打つと、その言葉は文字として残らず、溶けて粒子、群れ、粘菌、音になる。  
例として、叫びは荒く震え、眠さは沈み、反復は分裂し、感嘆符は破裂し、三点リーダーは静かに滲む。ただし、これは感情推定ではなく、文字のふるまいの物理変換。

### 3. 採用する名前・表記

- 正式名: WordSlime
- 日本語副題: ことばの粘菌水槽
- キャッチコピー: ことばが溶けて、勝手に生きものになる。
- Protopedia向け説明では、意味解析しないこと、物理パラメータへ変換すること、言葉を読むのではなく飼うことを明確にする。

### 4. 絶対に守る設計思想

やることは、文字を粒子生命体にする、入力で物理挙動を変える、WebGPUで大量粒子を動かす、Web Audio APIで音も作る、完全ローカルで動かすこと。  
やらないことは、AI診断、感情推定、スコア化、文章要約、ChatGPT連携、SNS投稿前提、タスク管理、研究用ダッシュボード、文章保存の強制。

### 5. ターゲット体験

狙う体験は「なんか文字を打つと、変な生き物が生まれる」。  
ユーザーが感じるべき質感は、きもい、かわいい、ずっと見てしまう、意味がありそうでない、自分の言葉なのに他人っぽい、スクショを撮りたくなること。質感キーワードは、粘菌、スライム、水槽、胞子、群れ、微生物、文字の死骸、電子ペット、壊れた詩、意味の腐敗。

### 6. 画面構成

メインは全画面Canvas。入力欄は中央または下部に置き、右上に設定、左下に水槽状態、右下に保存系ボタンを置く。  
入力欄は普通のテキストボックスではなく召喚口。Enterで投入、Shift+Enterで改行、IME対応、投入直後に文字を表示し、0.5から1.5秒で崩壊して粒子化する。  
水槽は画面全体。古い言葉ほど沈み、新しい言葉ほど明るく反応が大きい。画面端は壁ではなく膜として扱う。設定は最小限で、粒子数、音、背景、挙動、保存、リセット、About。

### 7. 入力仕様

1回のEnterで1つの `WordSeed` を生成する。日本語、英語、数字、記号、絵文字、改行、空白を入力可能。  
1回最大280文字、空文字と空白のみは不可。超過分は末尾を切る。1秒間に5回以上の投入は内部キュー処理。暴言や意味不明文字列も禁止せず、アプリは内容を評価しない。

### 8. 文字列解析仕様

最重要章。意味解析ではなく、形態解析に近い物理変換。  
特徴量は `length`, `uniqueCharRatio`, `repeatRatio`, `punctuationRatio`, `whitespaceRatio`, `emojiRatio`, `hiraganaRatio`, `katakanaRatio`, `kanjiRatio`, `latinRatio`, `digitRatio`, `exclamationCount`, `questionCount`, `ellipsisCount`, `newlineCount`, `averageCharCodeDelta`, `rhythmVariance`。

変換先は `SlimeGenome`。`energy`, `viscosity`, `cohesion`, `separation`, `turbulence`, `decay`, `fertility`, `brightness`, `noiseScale`, `pitchBase`, `chirpRate` を持つ。

重要な変換規則:

- 長い文字列: 粒子数増、群れが大きく、動きは少し重い
- 同じ文字の反復: `fertility` と `chirpRate` 上昇、分裂しやすい
- 感嘆符: `energy` と `turbulence` 上昇、破裂
- 疑問符: `cohesion` 低下、`separation` 上昇、迷うように散る
- 三点リーダー、ピリオド反復: `energy` 低下、`viscosity` 上昇、沈む、低音
- ひらがな: 柔らかい、粘性高め、丸い粒子
- カタカナ: 角ばる、乾いた音、跳ねる
- 漢字: 重い、密度が高い、沈みやすい、中心核になりやすい
- 英字: 滑る、横方向に流れる、ノイズが細かい
- 数字: 周期運動、幾何学的配置、機械的な音
- 絵文字: 色相と形状変化、予測不能な変異

### 9. 粒子生命体仕様

最小単位は `Particle`。`seedId`, `position`, `velocity`, `acceleration`, `age`, `life`, `mass`, `radius`, `energy`, `genomeInfluence`, `state` を持つ。  
状態は `spawning`, `swimming`, `coalescing`, `splitting`, `sleeping`, `decaying`, `dead`。  
`WordSeed` は入力テキスト、特徴量、Genome、粒子数、発生位置、色、音の署名を持つ。粒子数は `24 + text.length * 8 + repeatRatio * 120 + emojiRatio * 80` を 24 から 1200 に clamp する。

### 10. シミュレーション仕様

粒子に働く力は、慣性、粘性抵抗、ノイズ流、群れへの引力、近接粒子からの斥力、中心回帰、マウス/タッチ反応、古い粒子への吸着、Seedごとの性格。  
Boids風の alignment/cohesion/separation を使うが、鳥の群れに寄せすぎず、液体、粘菌、微生物の中間を狙う。  
流体風ノイズ、同一Seed粒子の合体、反復や高エネルギーによる分裂、古い粒子の沈殿と再浮上が必要。

### 11. 描画仕様

Canvas全面描画、WebGPU優先、WGSLで粒子更新と描画。  
文字崩壊演出のみ Canvas2D または SDF テキストを併用。WebGPU非対応時は簡易Canvas版を作らず、非対応文言を出す。  
粒子は丸く、にじみ、個体差があり、中心が濃く外側が薄い。塊はmetaball風。背景は完全な黒ではなくノイズと沈殿レイヤーを持つ。文字崩壊は、表示、輪郭の震え、端から粒子漏れ、崩壊、粒子生命体化の流れ。

### 12. 音仕様

音源ファイルなし。Web Audio APIでプロシージャル生成。  
音は、投入時の `Spawn Sound`、低い環境音の `Slime Hum`、たまに鳴く `Chirp`、異なるSeed衝突時の `Collision Sound`。  
初期状態は音なし。ユーザー操作後に音を有効化し、ミュート常時表示。音量は控えめで、Loudだけ壊す。

### 13. 操作仕様

キーボードは Enter, Shift+Enter, Esc, Space, R, S, V, M, 1から5。  
マウスは移動で回避、クリックで波紋、長押しで吸引、ドラッグで流れ、ダブルクリックで渦。  
タッチはタップ、長押し、スワイプ、ピンチ。ただしピンチはズームではなく密度変化。

### 14. プリセット仕様

- Slime: 標準。粘性高め、合体しやすい、気持ち悪かわいい
- Swarm: 群れ。速い、まとまりやすい、Seedごとの群れがぶつかる
- Smoke: 煙。拡散、形が残りにくい、薄い音、スクショ映え
- Fungus: 粘菌。成長が遅い、枝分かれ、放置構造、一番WordSlimeらしい
- Glitch: 壊れ。数字と記号に強く反応、揺れ、電子音、制御されたバグ感

v1では Slime / Swarm / Smoke の3モードが必須で、Fungus / Glitch は後回しでよい。

### 15. 保存・共有仕様

PNG保存はv1必須。透かしなし、現在Canvasサイズ、ファイル名は `wordslime_YYYYMMDD_HHMMSS.png`。  
WebM録画は最大30秒、音あり/なし選択、MediaRecorder使用。JSON保存は水槽状態を保存。共有URLは初期版では実装しない。

### 16. アプリ状態

`AppState` は、モード、一時停止、音、粒子品質、Seeds、Particles、選択Seed、設定、パフォーマンスを持つ。  
モードは `slime`, `swarm`, `smoke`, `fungus`, `glitch`。粒子品質は `low`, `medium`, `high`, `insane`。

### 17. 技術スタック

採用は Vite, TypeScript, WebGPU, WGSL, Canvas, Web Audio API, Zustand または軽量自作store, CSS Modules または素のCSS, MediaRecorder, Blob export, optional File System Access API。  
採用しないものは Next.js、サーバー、DB、Supabase、LLM API、ログイン、認証、外部音源。Reactは必須にしない。Canvas中心なので Vite + TypeScript + 素の描画エンジンが一番合う。

### 18. ディレクトリ構成

要件では `wordslime/` 配下に `src/app`, `src/input`, `src/simulation`, `src/rendering/webgpu`, `src/audio`, `src/export`, `src/ui`, `src/styles` を置く構成。  
現リポジトリ直下が `C:\Projects\WordSlime` なので、実装時は直下をプロジェクトルートにするか、要件どおり `wordslime/` サブディレクトリを作るかを決める必要がある。リポジトリ名がすでに WordSlime なので、直下に `src/` を置く方が扱いやすい。

### 19. コアモジュール要件

- `TextFeatureExtractor`: 入力文字列から特徴量を出す純粋関数。DOM非依存、テスト可能、日本語と絵文字を壊さない。文字数はUnicode code point基準。
- `GenomeMapper`: 特徴量を `SlimeGenome` に変換。0から1正規化、完全決定的、同じ入力なら同じGenome、意味辞書なし。
- `ParticleSystem`: 粒子生成、更新、寿命管理。GPU更新が基本。CPU側状態は最小限。古い粒子はdecayへ。
- `Renderer`: WebGPU初期化、canvas context、particle buffer描画、背景ノイズ、bloom風、metaball風、resize、device lost対応。
- `AudioEngine`: ユーザー操作後に AudioContext 開始。ミュート、音量制限、ノード破棄、同時発音数制限。
- `ExportManager`: PNG/WebM/JSON保存、保存中UI、録画失敗時の短いエラー、スマホ非対応でも落ちない。

### 20. パフォーマンス要件

目標は Desktop Medium 10,000粒子/60fps、High 30,000粒子/60fps、Insane 100,000粒子/30fps以上。Mobile Low 2,000粒子/60fps、Medium 5,000粒子/30fps以上。  
fps低下時は bloom、metaball精度、背景ノイズ解像度、粒子数、音の発火頻度の順に下げる。  
長時間放置でメモリが増え続けないこと、死んだ粒子の再利用、毎フレームobject allocationを避けることが必須。

### 21. エラーハンドリング

WebGPU非対応時は「この水槽は、このブラウザではまだ育ちません。WebGPU対応ブラウザで開いてください。」と表示し、簡易Canvas版は作らない。  
AudioContext開始失敗時は音なしで続行。録画失敗時はPNG保存を促す。GPU device lost は自動復帰を試し、失敗時は再読み込み案内。

### 22. UI文言

初回、入力プレースホルダー、音オン、リセット確認、WebGPU非対応、録画中、保存完了の文言が指定されている。  
作品トーンに直結するため、UI文言は実装時に勝手に説明臭くしない。

### 23. デザイン仕様

余白は大きめ、UIは薄く、主役は水槽、文字は少なく、設定パネルは半透明、ボタンは小さめ、角丸は強すぎない。研究ツールっぽくしない。  
色は Default: Deep Tank、Milk、Glitch の方向性がある。v1の基本は Deep Tank がよい。

### 24. アクセシビリティ

音なしでも成立、点滅しすぎない、Reduce Motion対応、キーボード操作、コントラスト、入力欄のスクリーンリーダー認識が必須。  
Glitchでも高速点滅や画面全体フラッシュは禁止。

### 25. プライバシー・セキュリティ

入力した言葉は外に出さない。サーバー送信なし、外部API送信なし、ログインなし、Cookie不要、localStorage保存は設定のみ、入力文字列の自動保存なし。  
JSON保存はユーザーが明示したときだけ。作品説明にもブラウザ内完結を明記する。

### 26. 受け入れ基準

絶対条件は、入力投入、文字崩壊、粒子生成、常時動作、入力ごとの挙動変化、同一入力で同傾向、マウス反応、音、PNG保存、WebGPU非対応時に落ちない、サーバーなし、READMEで起動可能。  
作品条件は、10秒で「何これ」、30秒で別の言葉を入れたい、1分でスクショを撮りたい、説明なしで文字が生き物になったと分かる、実用アプリや研究ツールに見えないが技術的にはちゃんとしていること。

### 27. READMEに書くべき内容

READMEには、WordSlimeの説明、Features、Privacyを入れる。  
特に `It does not analyze meaning, emotion, or mental state.` と、typed text is processed locally を明記する。

### 28. 作品紹介文

短文、強い版、一番いい紹介文が用意されている。  
実装後のREADME、About、Protopedia用説明では「一番いい紹介文」を軸にするのがよい。

### 29. 最初に実装すべき最小完全仕様

v1必須:

- WebGPU初期化
- 全画面Canvas
- 入力欄
- TextFeatures計算
- Genome変換
- 粒子生成
- 粒子更新
- 粒子描画
- 文字崩壊演出
- マウス反応
- Slime / Swarm / Smoke の3モード
- 音オン/オフ
- Spawn Sound
- Slime Hum
- PNG保存
- リセット
- About
- README

v1で切ってよい:

- WebM録画
- JSON復元
- Fungusモード
- Glitchモード
- スマホ最適化
- File System Access API
- 高度なmetaball
- 10万粒子
- 共有URL

### 30. 面白さ

面白さの中心は、意味を扱っていそうで扱わないこと。  
AI時代のソフトが理解、分析、診断へ寄るのに対して、WordSlime は理解しない。読む前に溶かす。そこが作品性と技術性の両方の核。

## 技術調査結果

### WebGPU

- MDNでは WebGPU API は Limited availability 扱いで、利用時は `navigator.gpu`、`requestAdapter()`、`requestDevice()` の検出が必要。
- MDN browser-compat-data では、Chrome は 113 からデスクトップ中心で対応、Chrome Android は 121 から、Firefox は 141 から部分対応、Safari は 26 から対応とされる。
- Chrome の WebGPU はグラフィックアクセラレーション無効、GPUブロックリスト、OS/ハードウェア差で adapter が取れない場合がある。
- Chrome 146 で `featureLevel: "compatibility"` による compatibility mode が紹介されている。古いGPU/モバイル範囲を広げる可能性があるが、v1ではまず通常の `requestAdapter()` と明確な非対応表示を優先する。
- 要件どおり、WebGPU非対応時にCanvas2D簡易版は作らない。

参照:

- https://developer.mozilla.org/en-US/docs/Web/API/WebGPU_API
- https://raw.githubusercontent.com/mdn/browser-compat-data/main/api/GPU.json
- https://developer.chrome.com/docs/web-platform/webgpu/troubleshooting-tips
- https://developer.chrome.com/blog/new-in-webgpu-146

### WGSL

- WGSL は WebGPU のシェーディング言語。
- W3Cの最新公開版は 2026-05-19 の Candidate Recommendation Draft。
- 実装では `.wgsl` をViteの `?raw` import で文字列として読み込むのが自然。

参照:

- https://www.w3.org/TR/WGSL/
- https://vite.dev/guide/assets.html

### Vite / TypeScript

- Vite 公式は vanilla-ts テンプレートを提供している。
- Vite 8 系ドキュメントでは Node.js 20.19+ または 22.12+ が必要。ローカルは Node `v24.12.0` なので要件を満たす。
- PowerShellでは `npm.ps1` が実行ポリシーで止まるため、ローカル実行時は `npm.cmd` を使う。
- `index.html` はプロジェクトルートに置く前提。

参照:

- https://vite.dev/guide/
- https://vite.dev/guide/assets.html

### Web Audio API

- Web Audio はブラウザの autoplay policy の影響を受ける。ユーザー操作前に音を開始しない。
- 要件の「初期状態は音なし」「ユーザー操作後に音を有効化」は正しい。
- WebM録画で音も含める場合は、AudioContext の `createMediaStreamDestination()` で録音用ストリームを作り、canvas capture stream と合成する設計が候補。

参照:

- https://developer.mozilla.org/en-US/docs/Web/Media/Guides/Autoplay
- https://developer.mozilla.org/en-US/docs/Web/API/AudioContext/createMediaStreamDestination

### MediaRecorder / Canvas録画

- `MediaRecorder` は広く使えるAPIだが、MIME type はブラウザ差があるため `MediaRecorder.isTypeSupported()` で選ぶ。
- `HTMLCanvasElement.captureStream()` は Baseline Widely available。Canvasの映像をMediaStreamとして取れる。
- v1では録画は切ってよいため、設計だけ残し、実装はPNG保存を優先する。

参照:

- https://developer.mozilla.org/en-US/docs/Web/API/MediaRecorder
- https://developer.mozilla.org/en-US/docs/Web/API/HTMLCanvasElement/captureStream

### PNG / Blob保存

- Blob URL はローカル生成データのダウンロードに使える。
- Object URL は不要になったら `URL.revokeObjectURL()` で解放する。
- PNG保存は `canvas.toBlob()` からBlob URLを作り、`a[download]` で保存する方針がよい。

参照:

- https://developer.mozilla.org/en-US/docs/Web/URI/Reference/Schemes/blob

### File System Access API

- `showSaveFilePicker()` は Limited availability かつ experimental。secure context とユーザー操作が必要。
- 要件の「基本はBlobダウンロード、対応環境では直接保存」は妥当。
- v1では File System Access API を切ってよい。

参照:

- https://developer.mozilla.org/en-US/docs/Web/API/Window/showSaveFilePicker

### Unicode / 日本語 / 絵文字

- 要件では文字数は Unicode code point 基準。実装では `Array.from(text)` を基本にする。
- 絵文字や見た目上の1文字をさらに丁寧に扱うなら `Intl.Segmenter` の `granularity: "grapheme"` が候補。MDNでは Baseline 2024 Newly available。
- 日本語の単語分割は不要。意味解析を避ける要件なので、`Intl.Segmenter` は必要なら「文字の壊れにくさ」用途に限定する。
- Unicode property escapes は、ひらがな、カタカナ、漢字、ラテン、数字、記号、絵文字の比率計算に使える。ただし正規表現には `u` または `v` フラグが必要。

参照:

- https://developer.mozilla.org/en-US/docs/Web/JavaScript/Reference/Global_Objects/Intl/Segmenter
- https://developer.mozilla.org/en-US/docs/Web/JavaScript/Guide/Regular_Expressions/Unicode_Property_Escapes

### OffscreenCanvas

- OffscreenCanvas は Baseline Widely available で、Worker内描画にも使える。
- ただしv1ではWebGPUとUI入力を安定させるのが先。Worker化は後回しでよい。

参照:

- https://developer.mozilla.org/en-US/docs/Web/API/OffscreenCanvas

### Reduce Motion

- `prefers-reduced-motion` は Baseline Widely available。
- CSSだけでなくシミュレーション側でも速度、乱流、点滅、bloomを弱める設計が必要。

参照:

- https://developer.mozilla.org/en-US/docs/Web/CSS/@media/prefers-reduced-motion

## 実装上の重要判断

### プロジェクト構成

リポジトリがすでに `C:\Projects\WordSlime` なので、要件の `wordslime/` サブディレクトリをそのまま作るより、直下に `src/`, `index.html`, `package.json` を置く方が自然。READMEにもその前提で書ける。

### Store

Zustandは選択肢にあるが、React必須ではない。v1は軽量自作storeで十分。アプリ状態は素のTypeScriptオブジェクトとイベント購読で扱える。

### TextFeatures

最初にテスト可能な純粋関数として作る。ここは作品の心臓なので、実装前にテストケースを用意する。

最低テストしたい入力:

- `うわあああああ`
- `ねむい`
- `研究研究研究`
- `!!!`
- `???`
- `……`
- `...`
- `wwwwww`
- `ABC123`
- 絵文字連打
- 日本語、英語、数字、記号、空白、改行の混在

### GenomeMapper

意味辞書を使わず、完全決定的にする。  
ランダム性が必要な場合は、入力テキストからseedを作った seeded random のみ使う。

### 粒子データ

大量粒子を扱うため、CPU側でParticleオブジェクトを大量生成し続けない。  
実装は Structure of Arrays またはGPUバッファ中心にし、CPU側はSeed一覧と設定、統計だけ持つ。毎フレームのallocationを避ける。

### 近傍計算

30,000粒子以上で単純な全ペア近傍探索は重い。  
v1では、厳密Boidsよりも、Seed単位の中心、流れ場、簡易斥力、ポインタ反応で作品らしさを作る。将来、空間グリッドやタイル分割を足せる構造にする。

### metaball

高度なmetaballはv1で切ってよい。  
v1では加算描画、半透明円、bloom風ポストエフェクトで「溶けて合体して見える」表現を優先する。

### 文字崩壊

WebGPU粒子とは別に、投入直後の短い崩壊演出を Canvas2D で作るのが現実的。  
テキストを一度描き、揺れ、にじみ、粒子発生へ移行する。ここが体験の入り口なので、v1でも必須。

### 音

AudioContext はユーザー操作後に生成またはresumeする。  
粒子数ぶん音を作らず、Seed単位、密度統計、イベント単位で音を作る。同時発音数を制限する。

### IME

日本語入力中のEnterで誤投入しないよう、`compositionstart`, `compositionend`, `isComposing` を見る。  
これは入力体験に直結するのでv1必須。

### 保存

v1はPNGのみ。  
WebMとJSON復元は設計だけ残して未実装にする。PNGはBlob URLで保存し、保存完了文言「標本を保存しました。」を出す。

### WebGPU非対応

要件どおり代替描画を作らない。  
ただし、アプリが真っ白にならないよう、WebGPU初期化前にDOMの非対応表示を用意する。

## 実装順序

1. Vite + TypeScript の最小構成を作る
2. 全画面Canvas、UIレイヤー、入力欄を置く
3. `TextFeatures` とテストを作る
4. `GenomeMapper` とテストを作る
5. WebGPU初期化と非対応表示を作る
6. 粒子バッファ、初期生成、描画を作る
7. 粒子更新シェーダーと流れ場を作る
8. 文字崩壊演出を作る
9. Slime / Swarm / Smoke の3モードを作る
10. マウス反応を作る
11. AudioEngine の音オン/オフ、Spawn Sound、Slime Humを作る
12. PNG保存、リセット、Aboutを作る
13. READMEを書く
14. 受け入れ基準で確認する

## v1受け入れチェックリスト

- [ ] 文字を入力してEnterで投入できる
- [ ] 日本語IME中のEnterで誤投入しない
- [ ] 空文字と空白のみは投入されない
- [ ] 280文字を超えた入力は末尾を切る
- [ ] 投入した文字が画面上で一度表示される
- [ ] 文字が0.5から1.5秒で崩壊する
- [ ] 崩壊した文字から粒子が生成される
- [ ] 粒子が常時動く
- [ ] 入力ごとに挙動が変わる
- [ ] 同じ入力なら同じ傾向になる
- [ ] Slime / Swarm / Smoke を切り替えられる
- [ ] マウス移動、クリック、ドラッグに反応する
- [ ] 音をオンにするとSpawn Soundが鳴る
- [ ] 音をオンにするとSlime Humが鳴る
- [ ] ミュートできる
- [ ] PNG保存できる
- [ ] リセットできる
- [ ] Aboutを表示できる
- [ ] WebGPU非対応時に落ちず、指定文言を出す
- [ ] サーバーなし、外部APIなしで動く
- [ ] READMEだけで起動できる
- [ ] 実用アプリや研究ツールに見えない

## いまのローカル状況

- リポジトリ: `C:\Projects\WordSlime`
- 現在あるファイル: `要件定義書.md`, `.git`
- Gitブランチ: `main`
- コミット: まだなし
- `要件定義書.md`: staged の追加状態
- Node: `v24.12.0`
- npm: `11.6.2`
- PowerShell注意: `npm` ではなく `npm.cmd` を使う

## 次に作るもの

次の実作業は、要件の v1 に合わせて Vite + TypeScript の土台を作り、まず `TextFeatureExtractor` と `GenomeMapper` をテスト付きで実装すること。  
その後に WebGPU の粒子描画へ進む。ここを順番にやると、作品の核である「意味解析しない、ことばの粘菌水槽」を崩さずに進められる。
